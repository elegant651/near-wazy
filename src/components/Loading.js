import React from 'react'

import './Loading.scss'

const Loading = () => (
  <div className="Loading">
    <div      
      className="Loading__spinner"
      alt="loading"
    >Loading...</div>
  </div>
);

export default Loading;
